# Optional: Frontend API Base URL
# If omitted or left empty, Vite proxies requests from /api to the backend automatically.
# For direct API calls from other devices on the same Wi-Fi, you can optionally set:
# VITE_API_URL=http://<YOUR-PC-IP>:5000/api
VITE_API_URL=